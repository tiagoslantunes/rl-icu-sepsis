# RL ICU-Sepsis project — pinned dependencies (verified working set)
# Install:  pip install -r requirements.txt
# Note: torch is pinned to the CUDA build actually used; for CPU-only install
# `pip install torch` separately and remove the +cu line below.

gymnasium==1.2.3
icu-sepsis
stable_baselines3==2.8.0
optuna==4.8.0
scikit-learn==1.8.0
numpy==2.4.4
pandas==2.3.3
matplotlib==3.10.9
seaborn==0.13.2
# torch==2.12.0+cu130   # GPU build used during development (CUDA available)
torch
