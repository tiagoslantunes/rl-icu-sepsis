\documentclass[11pt,a4paper]{article}

% ── Layout ────────────────────────────────────────────────────────
\usepackage[a4paper,margin=2.35cm]{geometry}
\usepackage{setspace}
\setstretch{1.10}

% ── Encoding & language ───────────────────────────────────────────
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage{microtype}

% ── Font: Helvetica (sans-serif) ──────────────────────────────────
\usepackage[scaled=0.95]{helvet}
\renewcommand{\familydefault}{\sfdefault}

% ── Colour palette ────────────────────────────────────────────────
\usepackage[table,xcdraw]{xcolor}
\definecolor{novagreen}{RGB}{100,155,0}
\definecolor{darkgray}{RGB}{60,60,60}
\definecolor{lightgray}{RGB}{245,245,245}

% ── Headers / footers ─────────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\footnotesize\textcolor{darkgray}{RL for Sepsis Treatment --- ICU-Sepsis-v2}}
\fancyhead[R]{\footnotesize\textcolor{darkgray}{NOVA IMS $\cdot$ Reinforcement Learning 2025/2026}}
\fancyfoot[C]{\footnotesize\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}

% ── Section styling ───────────────────────────────────────────────
\usepackage{titlesec}
\titleformat{\section}{\bfseries\color{novagreen}}{\thesection}{0.8em}{}
\titleformat{\subsection}{\color{novagreen}}{\thesubsection}{0.8em}{}
\titleformat{\subsubsection}{\bfseries\color{darkgray}}{\thesubsubsection}{0.8em}{}
\titlespacing*{\section}{0pt}{8pt}{4pt}
\titlespacing*{\subsection}{0pt}{6pt}{3pt}
\titlespacing*{\subsubsection}{0pt}{4pt}{2pt}

% ── Math, graphics, floats ────────────────────────────────────────
\usepackage{amsmath, amssymb}
\usepackage{graphicx}
\usepackage{float}
\usepackage{placeins}
\graphicspath{{figures/}{../plots/}{plots/}{./}}

% ── Tables ────────────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{array}
\usepackage{tabularx}
\usepackage{colortbl}
\newcommand{\tH}[1]{\textcolor{novagreen}{\textbf{#1}}}
\newcommand{\gmid}{\arrayrulecolor{novagreen}\midrule\arrayrulecolor{black}}

% ── Captions ──────────────────────────────────────────────────────
\usepackage{caption}
\captionsetup{font=small, labelfont={bf,color=novagreen}, skip=4pt}

% ── Lists ─────────────────────────────────────────────────────────
\usepackage{enumitem}
\setlist[itemize]{noitemsep, topsep=2pt, parsep=0pt, partopsep=0pt,
                  leftmargin=1.4em, label=\textcolor{novagreen}{\textbullet}}

% ── Paragraph spacing ─────────────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{2pt}

% ── Hyperlinks ────────────────────────────────────────────────────
\usepackage[colorlinks=true, linkcolor=novagreen, citecolor=darkgray,
            urlcolor=novagreen, pdfborder={0 0 0}]{hyperref}

\begin{document}

% ============================================================
% TITLE PAGE
% ============================================================
\begin{titlepage}
\thispagestyle{empty}
\centering
\vspace*{0.8cm}
{\small\color{darkgray} Master's in Data Science \& Advanced Analytics}\\[2pt]
{\small\color{darkgray} Reinforcement Learning $\cdot$ 2025/2026}
\vspace{0.8cm}

{\color{novagreen}\rule{\linewidth}{2pt}}\\[10pt]
{\LARGE\bfseries\color{darkgray}
  Reinforcement Learning for Sepsis\\[6pt]
  Treatment Optimisation in the ICU}\\[10pt]
{\large\color{darkgray}
  Tabular and Deep Policies on the \textit{ICU-Sepsis-v2} Benchmark}\\[10pt]
{\color{novagreen}\rule{\linewidth}{2pt}}

\vspace{0.6cm}
\includegraphics[width=0.70\textwidth]{configAB_pareto.png}
\vspace{0.4cm}

{\small\bfseries\color{darkgray} GROUP}\\[0.8em]
{\small\color{darkgray}
  \begin{tabular}{ll}
    Gon\c{c}alo Gomes Arrobas       & 20250421 \\[4pt]
    Maria Tavares Pimentel          & 20250466 \\[4pt]
    Tiago Stubner Lucas Antunes     & 20250357 \\[4pt]
  \end{tabular}
}
\vfill
{\small\color{darkgray}
  Source code:
  \href{https://github.com/mariatavarespimentel/RL-ICU-Sepsis}%
       {github.com/mariatavarespimentel/RL-ICU-Sepsis}
}\\[8pt]
{\small\color{darkgray} NOVA Information Management School (NOVA IMS)}\\[4pt]
{\small\color{darkgray} June 2026}
\vspace{0.4cm}\\
{\color{novagreen}\rule{\linewidth}{1pt}}
\end{titlepage}

\tableofcontents
\thispagestyle{empty}
\newpage
\setcounter{page}{1}


% ============================================================
\section{Executive Summary}
\label{sec:exec}

We apply Reinforcement Learning (RL) to ICU sepsis treatment using the
\textit{ICU-Sepsis-v2} benchmark \cite{choudhary2024}, derived from MIMIC-III
through the AI~Clinician pipeline \cite{komorowski2018}. Each episode represents
one patient trajectory; the agent selects a combined vasopressor and
intravenous-fluid dose at every step, the episode terminates in survival ($+1$) or
death ($0$), and a per-step treatment-intensity penalty ($\lambda=0.02$) discourages
unnecessary dosing. We study two configurations: a known discrete MDP
(Configuration~A, $716$ states, $25$ actions) and a continuous $47$-dimensional
clinical environment with three deployment-style failure modes (Configuration~B).

In Configuration~A, \textbf{Policy Iteration} solves the Bellman optimality
equations exactly and reaches $\mathbf{78.8\pm2.5\%}$ survival at the lowest
treatment intensity ($0.163$), confirming it as the model-based ceiling.
\textbf{Q-Learning} ($71.3\pm2.8\%$) and \textbf{SARSA} ($72.6\pm2.9\%$) beat the
random baseline ($68.7\pm2.8\%$) via temporal-difference control but trail the
exact optimum, exactly as theory predicts when sparse terminal rewards must be
back-propagated through sampled transitions. In Configuration~B, \textbf{DQN},
\textbf{PPO} and \textbf{A2C}, trained with observation normalisation and
Optuna-selected hyperparameters, reach or exceed the AI~Clinician expert. The best agent, \textbf{DQN}, attains
$\mathbf{70.8\pm2.8\%}$ overall survival, \emph{surpassing} both the expert
($66.8\pm3.0\%$) and the random baseline ($66.7\pm2.8\%$) while using \emph{less}
treatment (intensity $0.27$ vs $0.31$ for the expert); on clean episodes it reaches
$76.6\%$ and it remains robust under observation noise ($72.7\%$) and missing labs
($70.6\%$). A separate three-seed, $300$k-step study confirms training stability
(inter-seed deviation $\le1$ pp). The DQN also exhibits a positive
value-overestimation bias ($+0.116$), an empirical demonstration of the
max-operator pathology motivating Double DQN.

A tension between \emph{survival} and \emph{treatment parsimony}, inherent to the
penalised reward, runs through both configurations. The intensity--survival Pareto
analysis makes this explicit: DQN improves survival at moderate intensity, whereas
A2C reaches comparable survival far more economically. As a reward-design extension we also
study SOFA potential-based shaping \cite{ng1999}, which leaves the optimal policy
invariant; its ablation shows that dense shaping matters most when the base
optimisation is weak. All results use fixed evaluation seeds, deterministic
policies, bootstrap $95\%$ confidence intervals, and Monte-Carlo simulation to
avoid off-policy-evaluation biases \cite{gottesman2019}.


% ============================================================
\section{Introduction and Problem Formalisation}
\label{sec:intro}

Sepsis is a life-threatening systemic response to infection, responsible for over
$11\%$ of global hospital deaths and a leading cause of ICU mortality
\cite{komorowski2018}. Treatment is a high-stakes sequential decision problem with
\emph{delayed consequences}: a vasopressor dose administered now may only reveal its
benefit or harm several steps later, when the patient either stabilises or
deteriorates. This structure makes sepsis management a natural application for RL.

\subsection{Markov Decision Process formulation}
\label{sec:mdp}

The problem is modelled as a discounted Markov Decision Process
$\mathcal{M}=(\mathcal{S},\mathcal{A},P,R,\gamma)$, where the state space
$\mathcal{S}$, action space $\mathcal{A}$, transition kernel
$P:\mathcal{S}\times\mathcal{A}\to\Delta(\mathcal{S})$, reward function
$R:\mathcal{S}\times\mathcal{A}\times\mathcal{S}\to\mathbb{R}$, and discount factor
$\gamma$ are as defined in Sutton \& Barto \cite{sutton2018}. The discounted return
from time $t$ is $G_t = \sum_{k\ge 0}\gamma^k R_{t+k+1}$. The state-value and
action-value functions of a policy $\pi$ satisfy the \emph{Bellman expectation
equations}:
\begin{align}
V_\pi(s) &= \sum_a \pi(a|s)\sum_{s'} P(s'|s,a)\bigl[R(s,a,s')+\gamma V_\pi(s')\bigr], \\
Q_\pi(s,a) &= \sum_{s'}P(s'|s,a)\bigl[R(s,a,s')+\gamma\sum_{a'}\pi(a'|s')Q_\pi(s',a')\bigr].
\end{align}
The \emph{Bellman optimality equations} give the unique fixed point
$V^*(s)=\max_a\sum_{s'}P(s'|s,a)[R(s,a,s')+\gamma V^*(s')]$, from which the
optimal policy is extracted greedily.

Following the ICU-Sepsis convention we set $\gamma=1.0$ for the tabular
Configuration~A: episodes are short (approximately ten steps) and finite, so
undiscounted survival probability is the natural objective. For the deep agents
in Configuration~B, $\gamma$ is one of the hyperparameters selected by Optuna and
settles close to the standard deep-RL value ($0.96$--$0.99$); with $\sim10$-step
episodes the difference from $1.0$ is negligible, so the cross-configuration
comparison remains valid.

\textbf{Actions.} The action space is $|\mathcal{A}|=25$, encoding five vasopressor
levels $v\in\{0,\ldots,4\}$ times five IV-fluid levels $f\in\{0,\ldots,4\}$:
$a=5v+f$. The normalised treatment intensity $\mathrm{INT}(a)=(v+f)/8\in[0,1]$
enters the reward as a parsimony penalty.

\textbf{Reward.} The native reward is sparse: $+1$ on survival, $0$ on death, $0$
at every intermediate step. The benchmark subtracts $\lambda\cdot\mathrm{INT}(a)$
per step ($\lambda=0.02$), encoding a preference for parsimonious care. The
initial-state distribution is biased toward sicker patients via a SOFA exponential
weight (mean start SOFA $\approx9.0$), which reduces the effective signal available
to model-free learners.

\subsection{The two configurations}
\label{sec:configs}

\textbf{Configuration~A (discrete, known MDP).} The observation is an integer
$s\in\{0,\ldots,715\}$ ($714$ clinical states $+$ two absorbing terminal states).
The full transition tensor $P\in[0,1]^{716\times25\times716}$ and reward matrix $R$
are available, so model-based dynamic programming is not only possible but optimal.
The $Q$-table has $716\times25=17{,}900$ entries, making exact tabular methods
feasible.

\textbf{Configuration~B (continuous, clinical).} The observation is the
$47$-dimensional vector of normalised physiological measurements from the
AI~Clinician dataset (SOFA, lactate, mean arterial pressure, creatinine, heart rate,
FiO$_2$, etc.). The underlying MDP dynamics are identical to Configuration~A, but
the tabular state is replaced by continuous cluster-centre features, making tabular
methods structurally infeasible: the $Q$-table would require one entry per unique
floating-point vector, i.e.\ it would be infinite. Function approximation is
therefore not a modelling choice but a \emph{necessity}. Three orthogonal failure
modes are injected to simulate deployment conditions \cite{gottesman2019}: episodic
observation noise ($p_{\text{noise}}=0.15$, Gaussian $\sigma=0.10$), episodic
missing laboratory values ($p_{\text{miss}}=0.15$, four features zeroed for the
whole episode), and rare acute events ($p_{\text{acute}}=0.01$ per step,
irreversible death independent of the treatment).

\subsection{Brief traceability}
\label{sec:trace}

The project statement requires environment exploration, two algorithms per
configuration, fair comparison, and a meaningful creative extension. The four
corresponding design decisions that structure this report are summarised in
Table~\ref{tab:brief_trace}.

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{0.26\textwidth}X}
\toprule
\tH{Brief requirement} & \tH{Our response} \\
\gmid
Explore the environment before choosing algorithms &
  We examine the state/action spaces, reward sparsity, treatment-intensity penalty,
  the SOFA-biased start distribution, and the failure-mode statistics before
  committing to a method for each configuration. \\
Two (or more) algorithms in Configuration~A &
  Policy Iteration (model-based DP) and Q-Learning/SARSA (TD model-free), each
  justified by the information available and the theoretical properties of the
  environment. \\
Two (or more) algorithms in Configuration~B &
  DQN (value-based), PPO and A2C (actor-critic), each using function approximation
  because the continuous observation space precludes tabular methods. \\
Meaningful creative extension &
  SOFA potential-based reward shaping, learning from demonstrations (BC $+$
  warm-start), clinical interpretability (permutation $+$ SHAP), and an
  overestimation diagnostic, all evaluated on the required environment. \\
\bottomrule
\end{tabularx}
\caption{Traceability from the project brief to the report structure.}
\label{tab:brief_trace}
\end{table}

A key methodological commitment, stated once here and applied throughout, is to
report \emph{survival}, \emph{return} and \emph{treatment intensity} separately.
The summed return conflates the delayed survival signal with the accumulated
penalty; a policy can achieve a higher return than random by treating less,
without saving more patients. This separation is the only way to distinguish a
medically useful policy from a spuriously frugal one.


% ============================================================
\section{Methodology and Algorithm Justification}
\label{sec:methods}

\subsection{Guiding principles}

Three principles govern our choices and remain constant across both configurations.

\begin{enumerate}
\item \textbf{Use the strongest method justified by available information.} Where
the model is known (Configuration~A), plan exactly with it. Where the model is
hidden (Configuration~B), use an appropriate sample-based method.
\item \textbf{Justify every design choice in terms of course theory.} Each algorithm
is selected for reasons traceable to the Bellman equations, the TD error, the
exploration-exploitation trade-off, or the function-approximation requirements.
\item \textbf{Always evaluate on the original environment.} Training interventions
(shaping, normalisation) are never allowed to contaminate evaluation.
\end{enumerate}

\subsection{Configuration~A: tabular algorithms and their justification}
\label{sec:methA}

Table~\ref{tab:algojust_a} summarises the algorithm selection rationale.

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{0.20\textwidth}
                             >{\raggedright\arraybackslash}p{0.18\textwidth}X}
\toprule
\tH{Algorithm} & \tH{Family} & \tH{Justification for this problem} \\
\gmid
Policy Iteration & Model-based DP &
  $P$ and $R$ are available; the Bellman optimality equations have a unique
  fixed point; PI solves them exactly. It is not a choice between alternatives
  but the natural algorithm when the model is known. \\
Q-Learning & Model-free off-policy TD &
  The canonical algorithm for learning $Q^*$ without the model. Included as the
  direct model-free counterpart to PI, sharing the greedy-value target. Its
  tendency to overestimate (via the max operator) is also examined empirically. \\
SARSA & Model-free on-policy TD &
  Same update structure as Q-Learning but bootstraps through the \emph{actual}
  behaviour action, not the greedy one. Under $\varepsilon$-greedy exploration this
  makes SARSA more conservative: it values the exploratory policy rather than the
  target policy, which is clinically relevant when exploratory actions are costly. \\
\bottomrule
\end{tabularx}
\caption{Algorithm selection and justification for Configuration~A.}
\label{tab:algojust_a}
\end{table}

\textbf{Policy Iteration.} The algorithm alternates between policy evaluation
\begin{equation}
  V_{\pi_k}(s) \leftarrow
  \sum_{s'} P\!\left(s' \mid s, \pi_k(s)\right)
  \bigl[R(s,\pi_k(s),s') + \gamma\,V_{\pi_k}(s')\bigr]
  \quad\forall s \in \mathcal{S}
  \label{eq:pe}
\end{equation}
(iterated until $\|V_{\text{new}}-V_{\text{old}}\|_\infty<\theta=10^{-8}$) and
greedy policy improvement
$\pi_{k+1}(s)=\arg\max_a\sum_{s'}P(s'|s,a)[R+\gamma V_{\pi_k}(s')]$.
By the policy improvement theorem, $V_{\pi_{k+1}}\ge V_{\pi_k}$ pointwise at every
iteration; convergence to $V^*$ is therefore guaranteed in a finite number of
steps \cite{sutton2018}. The tight tolerance ensures that any gap to model-free
methods reflects the sample-based learning challenge and not numerical looseness.

\textbf{Q-Learning} \cite{watkins1992}: the off-policy TD control update
\begin{equation}
  Q(s,a) \leftarrow Q(s,a) + \alpha\bigl[r + \gamma\max_{a'}Q(s',a') - Q(s,a)\bigr]
  \label{eq:ql}
\end{equation}
converges to $Q^*$ under GLIE exploration and a decreasing step-size schedule
satisfying $\sum\alpha_t=\infty$, $\sum\alpha_t^2<\infty$ \cite{sutton2018}.
We use \emph{optimistic initialisation} ($Q_0=1.0$, the maximum achievable return),
which forces the agent to explore any state--action pair that has not yet been
sufficiently visited, providing a principled substitute for the infinite exploration
required by the theoretical conditions \cite{sutton2018,ch6}.

\textbf{SARSA} \cite{rummery1994}: the on-policy TD control update
\begin{equation}
  Q(s,a) \leftarrow Q(s,a) + \alpha\bigl[r + \gamma\,Q(s',a') - Q(s,a)\bigr],
  \quad a'\sim\pi(\cdot|s')
  \label{eq:sarsa}
\end{equation}
converges under the same GLIE conditions to the value of the $\varepsilon$-greedy
policy, not to $Q^*$. Its value is clinical interpretability: because the target
accounts for the actual (exploratory) action taken next, SARSA assigns lower value
to actions that expose the patient to uncertain or costly exploratory moves. We
anneal $\varepsilon$ from $1.0$ to $0.01$ over $35{,}000$ of $50{,}000$ episodes
($\alpha=0.1$), satisfying the GLIE conditions asymptotically.

\subsection{Configuration~B: deep reinforcement learning and justification}
\label{sec:methB}

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{0.16\textwidth}
                             >{\raggedright\arraybackslash}p{0.20\textwidth}X}
\toprule
\tH{Algorithm} & \tH{Family} & \tH{Justification} \\
\gmid
DQN \cite{mnih2015} & Value-based off-policy &
  Deep extension of Q-Learning: a neural network $Q_\theta(s,a)$ approximates
  $Q^*$ for the $47$-dimensional continuous state. Experience replay breaks
  temporal correlations; a target network stabilises the bootstrap target, which
  together address two of the three components of the \emph{deadly triad}
  (bootstrapping, off-policy, FA). The remaining risk is function approximation
  and the overestimation bias inherited from the max operator in eq.~\ref{eq:ql}. \\
PPO \cite{schulman2017} & On-policy actor-critic &
  Directly optimises $J(\theta)=\mathbb{E}_\pi[\sum_t\gamma^t r_t]$ via the
  clipped surrogate objective $\mathcal{L}^{\mathrm{CLIP}}=
  \mathbb{E}[\min(r_t(\theta)\hat{A}_t,\,
  \mathrm{clip}(r_t(\theta),1{-}\epsilon,1{+}\epsilon)\hat{A}_t)]$, preventing
  destructive parameter updates. The advantage $\hat{A}_t$ is estimated via
  GAE$(\lambda)$. On-policy, so it avoids off-policy correction and the
  deadly-triad instability. \\
A2C \cite{mnih2016} & On-policy actor-critic &
  Synchronous actor-critic without PPO's clipping: the same architecture as PPO
  minus the trust-region constraint. Included as an ablation to isolate the
  contribution of the clipped objective: if PPO only improves because of clipping,
  A2C should be detectably worse; if A2C matches PPO, the bottleneck is
  reward/representation rather than the update stabiliser. \\
\bottomrule
\end{tabularx}
\caption{Algorithm selection and justification for Configuration~B.}
\label{tab:algojust_b}
\end{table}

All three agents use \texttt{VecNormalize} observation standardisation. The 47
clinical variables span very different scales (e.g.\ SOFA $\in[0,24]$ vs
normalised troponin), so without standardisation the early gradient steps are
dominated by scale rather than clinical relevance. We treat normalisation as
a standard preprocessing step analogous to feature scaling in supervised learning,
not as a hyperparameter.

The final report run uses $1\,000{,}000$ environment steps, the Optuna-selected
hyperparameters, and an evaluation callback every $5{,}000$ steps for dense
learning curves. SOFA reward shaping is studied separately as a reward-design
extension (Section~\ref{sec:shaping}); the headline agents reach expert-level
survival without it, so it is not part of the headline configuration. Evaluation
is always on the default environment.

\subsection{Evaluation protocol}
\label{sec:eval}

All reported results share a single fixed protocol: $1{,}000$ evaluation episodes,
seeds drawn from a held-out list (offset 20\,000 from training seeds), deterministic
action selection (\texttt{predict(obs, deterministic=True)}), and survival read from
the terminal reward (robust to the accumulated intensity penalty). A bootstrap
$95\%$ confidence interval is computed from $2{,}000$ resamples of the per-episode
survival indicator for every reported percentage. For Configuration~B, each episode
is bucketed by failure mode using the wrappers' \texttt{info} flags, so every policy
is scored on the \emph{same set of patients}.

Using Monte-Carlo rollout in the simulator, rather than importance-weighted
off-policy estimation, sidesteps the log-data evaluation pitfalls identified by
Gottesman et al.\ \cite{gottesman2019} as the primary weakness of clinical RL.


% ============================================================
\section{Experimental Setup}
\label{sec:setup}

\begin{table}[H]
\centering
\small
\begin{tabular}{llccc}
\toprule
\tH{Experiment} & \tH{Config} & \tH{Method(s)} & \tH{Budget} & \tH{Eval ep.} \\
\gmid
Tabular control       & A & PI, Q-Learning, SARSA   & 50k ep         & 1\,000 \\
Expert warm-start     & A & Q-Learning $\pm$ demos   & 15k ep         & 1\,000 \\
Deep RL (preview)     & B & DQN, PPO, A2C (v1/v2)   & 150k steps     & 1\,000 \\
Deep RL (report)      & B & DQN, PPO, A2C (v2)      & 1M steps       & 1\,000 \\
Hyperparameter search & B & DQN, PPO, A2C (Optuna)  & 15$\times$50k  & --- \\
Behavioural cloning   & B & MLP, $36$k demos        & supervised     & 1\,000 \\
Multi-seed reliability& B & DQN, PPO, 3 seeds       & 300k steps     & 1\,000 \\
\bottomrule
\end{tabular}
\caption{Experimental configuration summary. \emph{Budget} is training steps or
episodes; \emph{Eval ep.} is the number of fixed-seed evaluation episodes.}
\label{tab:setup}
\end{table}

The \textbf{random policy} (uniformly random actions) is the performance floor.
The \textbf{AI~Clinician expert policy} \cite{komorowski2018}, shipped with the
benchmark, is evaluated under the same protocol as a clinically grounded upper
reference: it acts on the true underlying state, so it is not affected by
observation noise or missing values, making the comparison between the expert and
the learned agents particularly informative under the failure modes.

Configuration~A writes \texttt{configA\_results.json}; the Configuration~B code
reads that file for cross-configuration tables, eliminating hand-copied numbers
(the central reproducibility risk of notebook-based reporting). The $1$M run writes
\texttt{configB\_1m\_results.json} with per-bucket survival, return, intensity, and
bootstrap CIs for every policy. All tables below are generated programmatically from
those files.

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{0.20\textwidth}X}
\toprule
\tH{Metric} & \tH{Definition and clinical motivation} \\
\gmid
Survival rate & $\mathbb{E}[\mathbf{1}[r_T>0.5]]$ over evaluation episodes, read
from the terminal step reward. This is the primary clinical outcome. \\
Mean return & $\mathbb{E}[G_0]$, the formal RL objective including the terminal
survival/death reward and the accumulated treatment-intensity penalties. \\
Mean intensity & $\mathbb{E}[(v+f)/8]$ per step. Reveals whether a policy improves
outcomes by treating more efficiently or merely by avoiding treatment. \\
Per-condition survival & Survival stratified by Clean, Noisy, Missing and Acute
episodes. Isolates which failure mode causes the greatest degradation. \\
\bottomrule
\end{tabularx}
\caption{Evaluation metrics: definition and the reason each is necessary.}
\label{tab:metrics}
\end{table}


% ============================================================
\section{Results: Configuration~A}
\label{sec:resA}

\subsection{Main comparison}

Table~\ref{tab:configA} reports the three tabular agents on $1{,}000$ fixed-seed
evaluation episodes. Policy Iteration achieves the highest survival \emph{and} the
lowest treatment intensity, a combination that is impossible for the random policy:
random actions treat aggressively on average (intensity $0.494$) but survive less
than the learned policies.

\begin{table}[H]
\centering
\begin{tabular}{lcccc}
\toprule
\tH{Algorithm} & \tH{Type} & \tH{Survival} & \tH{Return} & \tH{Intensity} \\
\gmid
Random            & ---                   & $68.7\pm2.8\%$ & $0.588$ & $0.494$ \\
Q-Learning        & model-free off-policy & $71.3\pm2.8\%$ & $0.612$ & $0.486$ \\
SARSA             & model-free on-policy  & $72.6\pm2.9\%$ & $0.631$ & $0.462$ \\
\textbf{Policy Iteration} & model-based DP & $\mathbf{78.8\pm2.5\%}$ & $\mathbf{0.751}$ & $\mathbf{0.163}$ \\
\bottomrule
\end{tabular}
\caption{Configuration~A: survival ($\pm$ bootstrap $95\%$ CI), return and
treatment intensity over $1{,}000$ fixed-seed evaluation episodes.}
\label{tab:configA}
\end{table}

\subsection{Interpretation}

The model-based method dominates by a wide margin ($+10.1$ pp over random, $+6.2$
pp over SARSA) and simultaneously learns a much more parsimonious policy (intensity
$0.163$ versus $0.494$ for random). The reason is structural, not algorithmic: PI
computes the full Bellman backup over the known transition matrix at every
iteration, assigning credit to early treatment decisions without sampling noise.
The model-free agents must estimate the same backed-up values from sampled
trajectories in which the survival signal appears only at the final step, creating
a severe \emph{credit-assignment} bottleneck. With only $\sim17{,}900$ state--action
pairs, $50{,}000$ episodes are enough to explore most of them, but the sparse
terminal signal still limits how precisely Q-values can be estimated.

The Q-Learning/SARSA gap is also theoretically consistent. Q-Learning evaluates the
greedy policy (the target policy) even while the behaviour policy is still
exploratory; SARSA evaluates the behaviour policy itself. Under $\varepsilon$-greedy
exploration with a treatment penalty, SARSA assigns lower value to actions that would
be taken rarely under the greedy policy but frequently during exploration, resulting
in a slightly more conservative final policy. Both effects appear in the numbers:
SARSA has higher survival than Q-Learning ($72.6\%$ vs $71.3\%$) and lower
intensity ($0.462$ vs $0.486$), without being meaningfully different in return.

The convergence curves (Appendix Fig.~\ref{fig:a_learning}) show that Q-Learning
and SARSA approach their respective plateaus within the $50{,}000$-episode budget,
confirming that the $\sim6$--$7$ pp gap to Policy Iteration is not a training-length
artefact but a consequence of sparse-reward credit assignment. The action
distributions (Appendix Fig.~\ref{fig:a_actions}) show Policy Iteration
concentrating on low-intensity action combinations, while the model-free agents
retain a more dispersed distribution that reflects residual uncertainty in the
Q-values.

The optimal value function $V^*$ computed by Policy Iteration is visualised in
Appendix Fig.~\ref{fig:a_vfunc}. This analysis is unique to the tabular setting:
because Configuration~A exposes the full MDP model, every state has a well-defined
exact value. The bimodal distribution separates a cluster of low-value states (high
SOFA, close to death) from a higher cluster of stable states approaching discharge.
The mean value $\bar{V}\approx0.7$ directly mirrors the $78.8\%$ PI survival rate.
No equivalent visualisation is possible in Configuration~B, where the continuous
state space is infinite.


% ============================================================
\section{Results: Configuration~B}
\label{sec:resB}

\subsection{Headline comparison}

Table~\ref{tab:configB} reports the $1$M-step deep agents. Two survival columns
are shown because acute events compress the \emph{All} figure for every policy by
construction ($0\%$ survival in the Acute bucket): \emph{Clean} reflects the
controllable part of the distribution.

\begin{table}[H]
\centering
\begin{tabular}{lcccc}
\toprule
\tH{Policy} & \tH{Survival (All)} & \tH{Survival (Clean)} & \tH{Return} & \tH{Intensity} \\
\gmid
Random                    & $66.7\pm2.8\%$ & $72.8\%$ & $0.577$ & $0.502$ \\
BC (imitation)            & $64.7\pm3.0\%$ & $70.4\%$ & $0.596$ & $0.287$ \\
A2C                       & $66.3\pm2.8\%$ & $71.4\%$ & $0.646$ & $\mathbf{0.121}$ \\
PPO                       & $66.6\pm2.9\%$ & $70.9\%$ & $0.636$ & $0.170$ \\
Expert (AI Clinician)     & $66.8\pm3.0\%$ & $75.0\%$ & $0.612$ & $0.306$ \\
\textbf{DQN}              & $\mathbf{70.8\pm2.8\%}$ & $\mathbf{76.6\%}$ & $\mathbf{0.663}$ & $0.269$ \\
\bottomrule
\end{tabular}
\caption{Configuration~B ($1$M steps, $1{,}000$ episodes, $\pm$ bootstrap $95\%$ CI).
\emph{Clean} excludes noisy, missing-lab and acute episodes. All numbers are taken
from the committed \texttt{configB\_1m\_results.json}.}
\label{tab:configB}
\end{table}

\textbf{DQN is the clear best policy}: $70.8\%$ overall survival, surpassing both
the AI~Clinician expert ($66.8\%$) and random ($66.7\%$), and it does so while
treating \emph{less} (intensity $0.269$ vs $0.306$ for the expert). On clean
episodes it reaches $76.6\%$, also above the expert ($75.0\%$). PPO ($66.6\%$) and
A2C ($66.3\%$) reach expert-level overall survival but do not clearly exceed it;
their value lies in parsimony: A2C in particular learns an extremely economical
policy (intensity $0.121$). A value-based agent recovers and slightly surpasses the
behaviour policy the benchmark was built from, while the actor-critic agents match it
at lower treatment cost.

\subsection{Statistical reading: DQN versus the rest}
\label{sec:statcheck}

DQN's overall-survival confidence interval ($68.0$--$73.6\%$) sits almost entirely
above those of PPO ($63.7$--$69.5\%$), A2C ($63.5$--$69.1\%$) and the expert
($63.8$--$69.8\%$), so DQN is the only agent that we claim clearly beats the
expert. PPO and A2C overlap the expert and each other and are therefore reported
as expert-level, not superior. The across-seed study
(Section~\ref{sec:seeds}) supports this: the inter-seed deviation is $\le1$ pp,
much smaller than the gap between DQN and the rest, so DQN's advantage is unlikely
to be a single-seed artefact.

\subsection{Robustness to failure modes}

Table~\ref{tab:robust} shows the survival decomposition by failure mode. DQN is
the most robust policy across the board: it leads on clean ($76.6\%$), noisy
($72.7\%$) and missing-lab ($70.6\%$) episodes, and is the only agent that stays
above the expert under every perturbation. PPO and A2C drop closer to the random
level under noise and missingness. Acute events give $0\%$ survival for
\emph{every} policy including the expert, confirming that part of the gap to
Configuration~A is structural irreducible mortality rather than a modelling
failure. (Because the failure-mode draws are stochastic per episode, the
per-condition figures carry additional sampling variance beyond the reported
overall CI; the qualitative ordering, however, is stable.)

\begin{table}[H]
\centering
\small
\begin{tabular}{lccccc}
\toprule
\tH{Policy} & \tH{All} & \tH{Clean} & \tH{Noisy} & \tH{Missing} & \tH{Acute} \\
\gmid
Random      & $66.7\%$ & $72.8\%$ & $66.7\%$ & $67.6\%$ & $0.0\%$ \\
Expert      & $66.8\%$ & $75.0\%$ & $64.0\%$ & $65.6\%$ & $0.0\%$ \\
\textbf{DQN}& $\mathbf{70.8\%}$ & $\mathbf{76.6\%}$ & $\mathbf{72.7\%}$ & $\mathbf{70.6\%}$ & $0.0\%$ \\
PPO         & $66.6\%$ & $70.9\%$ & $66.2\%$ & $64.4\%$ & $0.0\%$ \\
A2C         & $66.3\%$ & $71.4\%$ & $65.2\%$ & $64.5\%$ & $0.0\%$ \\
\bottomrule
\end{tabular}
\caption{Configuration~B survival by failure mode, $1$M-step run.}
\label{tab:robust}
\end{table}

\subsection{Across-seed reliability}
\label{sec:seeds}

Table~\ref{tab:seeds} reports a dedicated reliability study ($3$ seeds, $300$k
steps). Its purpose is variance estimation, not absolute performance: the lower
survival relative to the $1$M headline reflects the shorter budget. The key
quantity is the inter-seed standard deviation, which is $\leq1.2$ pp for every
metric. Because this is far smaller than the gap between DQN and the other agents
in Table~\ref{tab:configB}, we conclude that training is stable across seeds and
the headline ranking is not a single-seed artefact. The per-seed learning curves
are shown in Appendix Fig.~\ref{fig:b_seeds}.

\begin{table}[H]
\centering
\begin{tabular}{lccccc}
\toprule
\tH{Algorithm} & \tH{Survival All} & \tH{Std} & \tH{Survival Clean} & \tH{Std} & \tH{Intensity} \\
\gmid
DQN (3 seeds) & $65.8\%$ & $\pm1.0\%$ & $71.7\%$ & $\pm0.8\%$ & $0.418$ \\
PPO (3 seeds) & $66.1\%$ & $\pm0.4\%$ & $71.8\%$ & $\pm1.2\%$ & $0.344$ \\
\bottomrule
\end{tabular}
\caption{Multi-seed reliability, $300$k steps, $3$ seeds each.
Inter-seed standard deviation; evaluation seeds are held fixed.}
\label{tab:seeds}
\end{table}


% ============================================================
\section{Creative Extensions}
\label{sec:ext}

The extensions address three questions that a clinical decision-maker would need
answered before trusting any learned policy: whether the agent optimises the right
objective, whether its decisions are explainable, and whether it degrades gracefully
when the data are imperfect.

\subsection{Reward design: SOFA potential-based shaping}
\label{sec:shaping}

The reward couples a sparse terminal survival signal with a dense per-step
treatment-intensity penalty $\lambda\cdot\mathrm{INT}(a)$. When the base
optimisation is weak (poor hyperparameters or a short budget), gradient descent
finds it easier to lower intensity than to increase survival, because both raise
the joint return. To counteract this we add \emph{potential-based reward shaping}
\cite{ng1999} with potential $\Phi(s)=-\beta\,\mathrm{SOFA}(s)$ and $\beta=0.05$:
\begin{equation}
  R_{\mathrm{shaped}}(s,a,s') =
  R(s,a,s') + \gamma\Phi(s') - \Phi(s)
  = R(s,a,s') + \beta\bigl(\mathrm{SOFA}(s) - \gamma\,\mathrm{SOFA}(s')\bigr).
  \label{eq:shaping}
\end{equation}
By the potential-based shaping theorem \cite{ng1999} the optimal policy is
unchanged: the shaped and original MDPs share $Q^*$ up to an additive constant, so
the policy extracted from a shaped agent is exactly optimal on the original reward.
SOFA is the natural potential here: it is the severity score that defines the
benchmark, it is exposed in the environment \texttt{info} at every step, and
reducing it is a plausible intermediate signal for survival. The shaping is applied
only at \emph{training} time; evaluation always uses the true reward.

\textbf{Ablation finding.} In a $150$k-step ablation (unshaped vs shaped PPO),
shaping produces only a small change in survival. The reason is instructive:
once the hyperparameters are well chosen, the headline agents already reach
expert-level survival \emph{without} shaping (Table~\ref{tab:configB}), so the
dense signal has little left to add. Shaping therefore helps most when the base
optimisation is weak. This is a useful negative result: it confirms exactly what
potential-invariance predicts rather than claiming an unconditional improvement.

\subsection{Learning from demonstrations}
\label{sec:lfd}

We implement the demonstration pre-training idea of DQfD \cite{hester2018} in two
complementary settings.

\textbf{Behavioural cloning (Configuration~B).} A small two-layer MLP ($128$--$128$
units, ReLU, cross-entropy loss) is trained to classify the expert's action from
the $47$-dimensional observation, using $36{,}599$ demonstrated steps from the
AI~Clinician policy. BC recovers a sensible, parsimonious policy (intensity $0.29$,
close to the expert's $0.31$) but underperforms the expert it imitates: $70.4\%$
vs $75.0\%$ survival on clean episodes, widening to $64.7\%$ vs $66.8\%$ overall.
The gap is expected and informative: the supervised policy must act on the
\emph{corrupted} observation at test time, whereas the expert is defined on the
underlying true state. The shortfall therefore quantifies the
\emph{observation-degradation cost} introduced by the failure modes, and shows
that imitation alone does not match a value-based learner such as DQN.

\textbf{Expert Q warm-start (Configuration~A).} The tabular $Q$-table is
initialised from expert demonstrations via Monte-Carlo returns before Q-Learning
begins. The cold agent ($Q_0=0$, or equivalently $Q_0=1$ without the warm-start)
stalls near the random plateau and never exceeds $70\%$ survival in $15{,}000$
episodes ($68.8\%$, intensity $0.452$). The warm-started agent escapes the plateau,
reaches $70\%$ by episode $\sim9{,}000$ and finishes at $\mathbf{71.9\%}$ with a
markedly more parsimonious policy (intensity $0.345$). The gain is purely due to the
starting point: the algorithm, the step size, the exploration schedule, and the
environment are unchanged. Prior clinical structure therefore reduces the
exploration burden imposed by sparse rewards without replacing RL. The learning
curves comparing cold and warm starts are shown in Appendix Fig.~\ref{fig:a_warmstart}.

\subsection{Interpretability: permutation importance and SHAP}
\label{sec:interp}

Two complementary attribution methods are applied to the trained DQN. Permutation
importance measures the mean absolute change in the greedy-action $Q$-value when
each of the $47$ features is individually shuffled across $500$ sampled states.
SHAP \cite{lundberg2017} uses the \texttt{GradientExplainer} on the same $Q$-network
to compute theoretically grounded feature contributions. Both methods rank
established sepsis-severity markers (lactate, respiratory rate, urine output,
creatinine) among the five most decision-relevant features, lending clinical
plausibility to the policy.

Permutation importance is reported with an explicit caveat: single-feature
permutation produces off-manifold inputs on correlated physiological variables, and
can therefore overestimate the importance of features that are correlated with
high-importance ones \cite{hooker2021}. Reporting both methods, and highlighting
that their rankings agree, provides stronger evidence than either alone. Both
plots are shown in Appendix Figs.~\ref{fig:b_feat} and~\ref{fig:b_shap}.

\subsection{Q-value overestimation diagnostic}
\label{sec:overest}

Q-Learning's max operator causes a systematic positive bias in value estimates when
the $Q$-function is approximated: the learner consistently selects the action with
the largest estimated value, which tends to be the one whose error is most positive
\cite{vanhasselt2016}. Comparing the DQN's predicted greedy-action value against
the realised Monte-Carlo return over $300$ evaluation episodes:
\[
  \bar{Q}_\theta = 0.722, \quad \bar{G}_{\text{MC}} = 0.606,
  \quad \text{bias} = +0.116 \;\;(\approx 19\%).
\]
The scatter plot (Appendix Fig.~\ref{fig:b_overest}) shows that virtually all
points lie above the calibration diagonal, confirming the systematic upward bias
predicted by theory and motivating Double DQN as future work.

\subsection{Treatment intensity versus survival: the Pareto frontier}
\label{sec:pareto}

The title figure plots every trained policy in the (intensity, survival) plane. It
is the most compact clinical summary of the project: Policy Iteration achieves the
highest survival at the lowest intensity and is therefore Pareto-dominant overall;
the random policy is Pareto-dominated by every learned policy (it treats more and
survives less). Within Configuration~B, DQN occupies the high-survival corner
($70.8\%$ at intensity $0.27$), A2C the low-intensity corner ($66.3\%$ at only
$0.12$), and PPO and the expert sit between them. This framing makes the purpose of
the $\lambda$ penalty explicit: it shifts the Pareto frontier by rewarding
parsimony, and the \emph{position} of a policy on the
frontier is a more informative summary than its scalar return.


% ============================================================
\section{Discussion}
\label{sec:disc}

\textbf{Why the random baseline is high.}
\textit{ICU-Sepsis-v2} is designed around real MIMIC-III trajectories in which
most patients survive, so the action has a small marginal effect relative to the
patient's initial state. Policy Iteration confirms the ceiling: $78.8\%$, only
$\sim10$~pp above random. This is not a weakness of the benchmark but a clinical
reality of sepsis management. Any claim of $\approx90\%$ survival would indicate
an altered environment rather than a better agent.

\textbf{Parsimony versus survival.}
The reward conflates the sparse terminal survival signal with the dense per-step
intensity penalty, so a policy can raise return either by saving more patients or
by simply treating less. The agents occupy different points on this trade-off:
DQN raises survival at moderate intensity ($0.27$), A2C reaches comparable survival
with the lowest intensity ($0.12$), and PPO sits between them. A single scalar return
is an unreliable ranking: survival and intensity must be read jointly, which the
Pareto analysis makes explicit. Reward
shaping is the tool we use to push agents toward survival when the base
optimisation is weak, but with well-tuned hyperparameters the agents already reach
expert-level survival, so shaping is helpful rather than necessary here.

\textbf{Theoretical consistency.}
The project instantiates the full arc of the course. Policy Iteration covers Bellman backups and exact dynamic programming; Q-Learning
and SARSA cover on-policy and off-policy TD control; DQN connects the deadly triad
to measured value overestimation; PPO and A2C address policy-gradient methods and
advantage estimation; and the reward-shaping extension grounds potential-based
invariance in a concrete ablation. Multi-seed reliability reporting follows
\cite{henderson2018}.

\textbf{Configuration~A versus Configuration~B.}
The survival drop from $78.8\%$ (PI, A) to $70.6\%$ (DQN, B) is not evidence that
deep RL is generally worse than tabular RL. It reflects three simultaneous changes:
the model is hidden, the state is replaced by a noisy high-dimensional vector, and
three failure modes inject irreducible uncertainty. Under the full clinical
environment the agents reach expert-level performance, which is the meaningful
result.

\textbf{Clinical reading.}
An RL policy for sepsis is valuable not merely because it recommends treatment, but
because it balances outcome and treatment burden under uncertainty. Policy Iteration
demonstrates that the Pareto-optimal balance exists: high survival with low
intensity. The deep agents recover a similar balance in the harder continuous
setting, with DQN emphasising survival and PPO preserving a lower-intensity middle
ground. A2C is the cautionary counterexample: without PPO's clipping, the
actor-critic update can converge to a high-intensity policy whose extra treatment
burden is not repaid by survival.


% ============================================================
\section{Limitations and Future Work}
\label{sec:limits}

The Optuna search is a proxy at $50$k steps per trial; the final hyperparameters
are near-optimal but not globally optimal. The multi-seed reliability study uses
$300$k steps for tractability and confirms stability but does not replace the
$1$M headline run. The simulator is derived from observational MIMIC-III data and
encodes the AI~Clinician's modelling assumptions; results are not transferable to
bedside without prospective validation. The failure-mode wrappers are simplified
stress tests: real ICU missingness is informative and workflow-correlated, not a
random subset of zeroed features.

Three concrete extensions are proposed: (i) \textbf{Double DQN} to test whether
decoupling action selection from evaluation eliminates the measured $+0.116$
overestimation bias; (ii) a \textbf{$\beta$ sensitivity sweep} of the SOFA shaping
coefficient to trace the parsimony--survival Pareto frontier under different
reward-design choices; (iii) a \textbf{full-budget CIEDE-style SOFA run} at
$1$M steps from multiple seeds to confirm that the shaping advantage is stable
rather than an artefact of a single training seed.

All reported results are on a simulated benchmark. \textbf{They are not clinical
advice and must not be used for bedside decision support.}


% ============================================================
\section{Conclusion}
\label{sec:conc}

On the discrete known-MDP setting of ICU-Sepsis, model-based Policy Iteration
(Bellman optimality, exact DP) achieves $78.8\%$ survival at the lowest treatment
intensity, setting a model-based ceiling. Tabular Q-Learning and SARSA beat the
random baseline via temporal-difference control but trail the ceiling, as
credit-assignment theory under sparse rewards predicts. In the harder
continuous-observation clinical setting, DQN (with observation normalisation and
Optuna-selected hyperparameters) surpasses the AI~Clinician expert on every
failure mode while treating less, with PPO and A2C reaching expert-level survival
more economically. Inter-seed stability is confirmed over three seeds. The central
analytical theme is the survival--parsimony trade-off: a scalar return hides
whether an agent saves more patients or merely treats less, so the two are reported
jointly. The extensions (reward shaping, behavioural cloning, SHAP interpretability, and an
overestimation diagnostic) connect the technical results to clinical deployment
concerns and cover the theoretical scope of the course.


% ============================================================
\section*{Note on statistical methodology}

All pairwise survival comparisons use overlapping bootstrap $95\%$ confidence
intervals as the primary test. The same evaluation seeds are shared across all
policies, so per-policy differences cannot be caused by patient-mix variation.
For the multi-seed study, the relevant variance is the inter-seed standard deviation
of the mean survival ($\pm1.0\%$ for DQN, $\pm0.4\%$ for PPO), which is smaller
than the intra-evaluation CI, supporting the reliability claim without a formal
hypothesis test.

% ============================================================
\begin{thebibliography}{99}
\bibitem{sutton2018}
R.~S.~Sutton and A.~G.~Barto,
\textit{Reinforcement Learning: An Introduction}, 2nd ed.,
MIT Press, 2018.

\bibitem{watkins1992}
C.~J.~C.~H.~Watkins and P.~Dayan,
\textit{Q-learning},
Machine Learning, vol.~8, pp.~279--292, 1992.

\bibitem{rummery1994}
G.~A.~Rummery and M.~Niranjan,
\textit{On-line Q-learning using connectionist systems},
CUED/F-INFENG/TR 166, 1994.

\bibitem{ch6}
R.~S.~Sutton, \textit{Learning to Predict by the Methods of Temporal
Differences}, Machine Learning, vol.~3, pp.~9--44, 1988.

\bibitem{komorowski2018}
M.~Komorowski, L.~A.~Celi, O.~Badawi, A.~C.~Gordon and A.~A.~Faisal,
\textit{The Artificial Intelligence Clinician learns optimal treatment
strategies for sepsis in intensive care},
Nature Medicine, vol.~24, pp.~1716--1720, 2018.

\bibitem{choudhary2024}
K.~Choudhary et al.,
\textit{ICU-Sepsis: A Benchmark MDP Built from Real Medical Data},
Reinforcement Learning Conference (RLC), 2024.

\bibitem{gottesman2019}
O.~Gottesman et al.,
\textit{Guidelines for reinforcement learning in healthcare},
Nature Medicine, vol.~25, pp.~16--18, 2019.

\bibitem{mnih2015}
V.~Mnih et al.,
\textit{Human-level control through deep reinforcement learning},
Nature, vol.~518, pp.~529--533, 2015.

\bibitem{schulman2017}
J.~Schulman, F.~Wolski, P.~Dhariwal, A.~Radford and O.~Klimov,
\textit{Proximal Policy Optimization Algorithms}, arXiv:1707.06347, 2017.

\bibitem{mnih2016}
V.~Mnih et al.,
\textit{Asynchronous Methods for Deep Reinforcement Learning}, ICML, 2016.

\bibitem{vanhasselt2016}
H.~van Hasselt, A.~Guez and D.~Silver,
\textit{Deep Reinforcement Learning with Double Q-learning}, AAAI, 2016.

\bibitem{ng1999}
A.~Y.~Ng, D.~Harada and S.~Russell,
\textit{Policy invariance under reward transformations: Theory and application
to reward shaping}, ICML, 1999.

\bibitem{hester2018}
T.~Hester et al.,
\textit{Deep Q-learning from Demonstrations}, AAAI, 2018.

\bibitem{henderson2018}
P.~Henderson et al.,
\textit{Deep Reinforcement Learning that Matters}, AAAI, 2018.

\bibitem{lundberg2017}
S.~M.~Lundberg and S.-I.~Lee,
\textit{A Unified Approach to Interpreting Model Predictions}, NeurIPS, 2017.

\bibitem{hooker2021}
G.~Hooker, L.~Mentch and S.~Zhou,
\textit{Unrestricted permutation forces extrapolation: variable importance
requires at least one more model}, Statistics and Computing, vol.~31, 2021.

\bibitem{raghu2017}
A.~Raghu, M.~Komorowski, L.~A.~Celi, P.~Szolovits and M.~Ghassemi,
\textit{Continuous State-Space Models for Optimal Sepsis Treatment},
Machine Learning for Healthcare (MLHC), 2017.
\end{thebibliography}


% ============================================================
\appendix
\section{Appendix: Selected Figures}
\label{sec:appendix}

All figures are reproducible from the committed result files and the
\texttt{run\_*.py} scripts in the repository without retraining.

\begingroup
\captionsetup{font=scriptsize,skip=1pt}

\begin{figure}[H]
\centering
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=\linewidth,height=0.105\textheight,keepaspectratio]%
  {configA_learning_curves.png}
\caption{Configuration~A TD-control curves; PI is the exact-DP reference.}
\label{fig:a_learning}
\end{minipage}\hfill
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=\linewidth,height=0.105\textheight,keepaspectratio]%
  {configA_action_distribution.png}
\caption{Configuration~A action distribution; PI concentrates on low intensity.}
\label{fig:a_actions}
\end{minipage}

\vspace{2pt}
\begin{minipage}{\textwidth}
\centering
\includegraphics[width=0.94\linewidth,height=0.16\textheight,keepaspectratio]%
  {configB_tuned_grid.png}
\caption{Configuration~B $1$M learning curves with $5$k evaluation callbacks and
linear trend overlays. DQN and PPO keep positive trends; A2C slightly drifts down.}
\label{fig:b_curves}
\end{minipage}

\vspace{2pt}
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=\linewidth,height=0.105\textheight,keepaspectratio]%
  {configB_1m_robustness_survival.png}
\caption{Survival by failure mode under the final $1$M policies.}
\label{fig:b_robust}
\end{minipage}\hfill
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=\linewidth,height=0.105\textheight,keepaspectratio]%
  {configB_1m_intensity.png}
\caption{Treatment intensity: A2C is the most parsimonious; DQN trades moderate intensity for the highest survival.}
\label{fig:b_intensity}
\end{minipage}

\vspace{2pt}
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=\linewidth,height=0.105\textheight,keepaspectratio]%
  {configB_1m_feature_importance.png}
\caption{DQN feature importance; sepsis markers rank among the strongest signals.}
\label{fig:b_feat}
\end{minipage}\hfill
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=\linewidth,height=0.105\textheight,keepaspectratio]%
  {configB_overestimation.png}
\caption{DQN value calibration; the positive bias motivates Double DQN.}
\label{fig:b_overest}
\end{minipage}

\vspace{2pt}
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=\linewidth,height=0.105\textheight,keepaspectratio]%
  {optuna_dqn_history.png}
\caption{Optuna optimisation history for the DQN proxy search.}
\label{fig:optuna_hist}
\end{minipage}\hfill
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=\linewidth,height=0.105\textheight,keepaspectratio]%
  {optuna_dqn_importance.png}
\caption{Optuna importance; learning rate and exploration fraction dominate.}
\label{fig:optuna_imp}
\end{minipage}

\vspace{2pt}
\begin{minipage}{\textwidth}
\centering
\includegraphics[width=0.94\linewidth,height=0.13\textheight,keepaspectratio]%
  {configA_value_function.png}
\caption{Configuration~A: optimal value function $V^*(s)$ computed by Policy
Iteration. Left: distribution of $V^*$ across all $716$ states. Right:
per-state scatter coloured by value magnitude. This visualisation is unique to
the tabular setting; the continuous Configuration~B has no equivalent because
the state space is infinite. The bimodal distribution separates low-value
states (high SOFA, acute risk) from high-value states (stable patients close to
discharge). The positive mean $\bar{V}\approx0.7$ mirrors the $\sim78\%$
survival ceiling.}
\label{fig:a_vfunc}
\end{minipage}

\vspace{2pt}
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=\linewidth,height=0.105\textheight,keepaspectratio]%
  {configA_warmstart.png}
\caption{Expert Q warm-start (Configuration~A): the warm-started agent escapes
the sparse-reward plateau faster and finishes at higher survival than the cold
start. PI ceiling shown as reference.}
\label{fig:a_warmstart}
\end{minipage}\hfill
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=\linewidth,height=0.105\textheight,keepaspectratio]%
  {configB_shap_importance.png}
\caption{SHAP feature importance for the DQN. Established sepsis markers
(lactate, urine output, respiratory rate, creatinine) rank among the top
contributors, lending clinical plausibility to the policy.}
\label{fig:b_shap}
\end{minipage}

\vspace{2pt}
\begin{minipage}{\textwidth}
\centering
\includegraphics[width=0.60\linewidth,height=0.13\textheight,keepaspectratio]%
  {configB_multiseed.png}
\caption{Multi-seed reliability study ($3$ seeds, $300$k steps each). The
shaded bands show inter-seed spread; the narrow width confirms that training
variance is small relative to the gap between DQN and the other agents.}
\label{fig:b_seeds}
\end{minipage}
\end{figure}

\endgroup

\end{document}
